AI AGENT-el kapcsolatban :
1 - Agent Heuristic: Amikor az AI ágens felfedez egy oldalt, be kell állítani egy logikát: if (urlPath.split('/').length > 2 || /\d/.test(urlPath)) return false; – ez megvédi az adatbázist a több ezer cikk beáramlásától a kategóriák közé.
2 - Source Management - Advanced Feature Ideas
    The AI Agent can use this dashboard to communicate back to the user, creating a two-way feedback loop:
    Health Status Reporting: If the AI agent detects a source has died (DOMAIN_DEAD) or put up a strict paywall, it can flag the source in the user's dashboard with a warning icon: "We haven't been able to reach this feed for 3 days. Do you want to update the link or remove it?"
    Algorithmic Discovery (Recommendations): Because the database maps exactly which users follow which combinations of sources, the AI can run clustering algorithms. The UI can have a "Suggested for You" section: "Users who follow Telex Tech and HWSW also highly rate Prohardver."
    Volume Balancing (Anti-Flood): Some sources publish 100 articles a day; others publish 2. The AI can analyze the volume and suggest to the user: "Source X is dominating your feed. Would you like me to summarize it strictly into bullet points to leave room for your other sources?"

Kodolassal kapcsolatban:
1 - URL Utility: Írni kell egy normalizeUrl(rawUrl: string): string függvényt a utils/ mappába, amit minden adatbázis-mentés előtt meghívsz.


TODO:
region calibration: a const countries kiszervezese kulon file-ba, es nyelvi lokalizacio alkalmazasa

FONTOS: ha nem is validaltam az email-t a regisztracio folyaman, akkor is be tudok lepni az email+jelszoval !!!!

IDEAS:
PROMPT BLUEPRINT: NuSift Jövőbeli Architektúra Fejlesztések

Kontextus az AI-nak:
Kérlek, vedd figyelembe az alábbi előzetesen egyeztetett architekturális terveket a NuSift projekthez. Jelenleg a [VÁLASZD KI A FEATURE-T: 1, 2 vagy 3] pont fejlesztését szeretném elkezdeni. Kérlek, add meg a szükséges Vue komponenseket, backend API végpont logikát és adatbázis séma módosításokat.
1. Custom Data Feeds (Source Manager Integráció)

    Koncepció: Ahelyett, hogy tetszőleges URL-eket scrapelnénk, a felhasználók meglévő forrásokhoz (Sources) csatolhatnak specifikus RSS/Atom feedeket. Ez csökkenti a szerver-terhelést és a felhasználók "crowdsource" módon építik az adatbázist.

    Validációs Logika (Szinkron):

        Azonnali HTTP GET kérés (max 3-5mp timeout).

        Sikeresség (200 OK) és megfelelő Content-Type (pl. application/rss+xml, text/xml) ellenőrzése.

        Könnyű XML parserrel a kötelező node-ok (<rss>, <channel>, <feed>) meglétének ellenőrzése.

    Kritikus Biztonság: SSRF (Server-Side Request Forgery) elleni védelem (privát IP-k szűrése, domain validáció a meglévő forrás domainjéhez kötve).

2. Sovereign Data Export (System Operations oldal)

    Koncepció: A "Sovereign Shield" elv maximális érvényesítése. A felhasználó 100%-ban birtokolja az adatait.

    Exportálható Adatkörök: Teljes érdeklődési gráf (Interest Graph), olvasási előzmények, kedvencként mentett cikkek, és a cikkekre adott személyes értékelések (Personal Ratings).

    Formátumok: * JSON (a könnyű hordozhatóságért és fejlesztői felhasználásért).

        CSV (az átláthatóságért és táblázatkezelős elemzésekhez).

3. Selective Agent Amnesia (System Operations oldal)

    Koncepció: Granuláris kontroll az AI ügynök tanulási memóriája felett, elkerülve a teljes "Hard Reset" szükségességét.

    Törlési Kritériumok:

        Időalapú: Pl. "Felejtsd el az elmúlt 7 nap olvasási szokásait" (ideális, ha a felhasználó beesett egy ideiglenes "rabbit-hole"-ba, pl. egy hétig csak a tőzsdét bújta).

        Téma/Kategória alapú: Pl. "Nullázd le az ügynök előítéleteit a 'Technológia' kategóriában".

    Adatbázis Hatás: A hírfolyamot generáló vektortér/súlyozás célzott visszatekerése (rollback) a megadott paraméterek alapján.





// parancsok:
    // npx prisma db push --force-reset     -> db full reset



class="glass-card bg-surface-container hover:bg-surface-container-high transition-colors rounded-xl p-4 md:p-5 flex flex-col gap-4 outline outline-1 outline-outline-variant/15 group w-full"


    jo, ezt most egyenlore nem csinalom meg, de kesobbiekben vegig fogunk 

menni az osszes file-on es megcsinaljuk mindenre kitterjedoen. most 

szeretnem ha az aktiv/inaktiv kartyakon lecserelned a jelenlegi 

suspend/activate/delete iconokat normalis gombokra. stilusukban legyenek

 hasonloak a dasboard oldalon lathato ai-nodeokhoz perce illeszkedve 

ebbe az oldalba is design szempontjabol. a delete gomb minden esetben 

dobjon egy modalt a confirm-ra. ha jol emlekszem az adatbazisban nem 

lesz ugyebar torolve de ghost state-be (vagy valami hasonlonak nevezted 

korabban) kerulnek, hogy kesobb ha vissza akarja adni a user, akkor ne 

kelljen ujra scrappelni es rss-t keresni ha mar volt korabban 

hozzarendelve. ird le ha van kerdesed vagy finimitasi otleted. a most 

feltoltott file-t vedd alapul ne a korabbi verzioit





JAVITASI JAVASLATOK:

URL Validáció (add.post.ts): Jelenleg a new URL(url) közvetlenül a try-catch blokk előtt vagy anélkül fut (attól függően, hol kapja el a hibát). Ha a kliens egy érvénytelen stringet küld (pl. "nem-url"), a Node.js beépített URL modulja kivételt dob, ami 500-as Internal Server Error-t eredményez. Ezt érdemes egy dedikált validációs lépésbe tenni (akár Zod használatával, akár egy natív try-catch-el), és egy tiszta 400 Bad Request hibával visszatérni.

Névkonvenció megtévesztés (toggle.put.ts): A payloadból kinyert változó neve sourceId (const { sourceId, isActive } = await readBody(event);), de a Prisma lekérdezés a UserSourceSubscription.id (vagy CategorySubscription) mezőjében keresi. Mivel a sémádban létezik tényleges sourceId is (ami a NewsSource-ra mutat), ez a jövőbeli karbantartásnál vagy a frontend bekötésnél bekavarhat. Érdemes átnevezni subscriptionId-ra, ahogy a delete végponton is csináltad.

Kódduplikáció csökkentése (Architekturális tipp): Minden végponton manuálisan olvasod ki a sütit, és validálod a JWT-t a jsonwebtoken segítségével. Mivel Nuxt 4-et használsz, érdemes lehet ezeket a lépéseket egy szerveroldali middleware-be (pl. server/middleware/auth.ts) kiszervezni, ami minden /api/user/ hívásnál lefut, és a dekódolt userId-t ráteszi az event.context.user-re. Ezzel végpontonként 10-15 sor boilerplate kódot spórolhatsz meg.


-------
Biztonsági Kockázatok (Akciópontok)
Kifejezett kérésedre átvizsgáltam a kódokat, és az alábbi, a szerveredet (és a Vercel Postgres adatbázisodat) potenciálisan megbénító sérülékenységeket azonosítottam:

    Adatbázis Kimerítés (DoS Vektor) - finalize-onboarding.post.ts: Ellenőrzöd ugyan, hogy a sources egy tömb-e, de a hosszát nem korlátozod. Egy rosszindulatú bot beküldhet egy 50 000 URL-t tartalmazó listát. Mivel a kódod egy for...of ciklusban egyesével, szinkron módon hajtja végre a Prisma upsert műveleteket, egy ekkora payload pillanatok alatt lefoglalja az összes adatbázis-kapcsolatot, és megbénítja a rendszeredet.

        Kritikus fix: Illessz be egy szigorú limitet a feldolgozás elé: if (sources.length > 50) throw createError(...).

    Insecure Direct Object Reference (IDOR) - rate-article.post.ts: Bár ez a fájl jelenleg ki van kommentezve, a benne lévő logika veszélyes. Pusztán a kérésben kapott id alapján frissíti a cikk pontszámát (score), az értékelő felhasználó jogosultságának vagy identitásának ellenőrzése nélkül. Egy támadó egy egyszerű iterációs scripttel felülírhatja a teljes hírfolyamod pontozási rendszerét.

        Kritikus fix: Amikor élesíted, a pontozást kizárólag az ArticleRating kapcsolótáblán keresztül szabad menteni, a JWT-ből kinyert userId szigorú bevonásával (ahogy azt az adatbázis sémádban amúgy nagyon helyesen meg is tervezted).

    Tárhely Kimerítés (Payload Bloat) - update-interests.post.ts és finalize-onboarding.post.ts: Nincs validálva a beérkező interests JSON objektum mérete és mélysége. Egy támadó extrém méretű JSON fákat küldhet be, ami indokolatlanul felfalja az adatbázisod tárhelyét és lassítja a lekérdezéseket.

        Kritikus fix: Limitálni kell az elvárható kulcs-érték párok számát a tömbben, ideálisan egy validációs könyvtár (pl. Zod vagy Yup) bevonásával a mentés előtt.

--------
Action Items / Performance & Environment Tweaks

    Vercel Execution Timeout (seed-region.post.ts): You are correctly using event.waitUntil() to create a fire-and-forget background job. However, be aware that Vercel enforces strict maximum execution limits on serverless functions (typically 10 seconds for the Hobby tier, up to 60 seconds for Pro). Your script iterates up to 10 pages with a forced 2-second delay between each (await new Promise(resolve => setTimeout(resolve, 2000))). This alone takes 20+ seconds, meaning Vercel will likely kill the process silently before it finishes writing the JSON file if you are on the Hobby tier.

        Fix: Consider lowering MAX_PAGES, moving the seeding to a local script, or using Vercel Cron Jobs to trigger smaller, bite-sized updates.

    Edge Runtime Compatibility (verify-source.post.ts): You are using node:dns/promises for fallback validation. If you ever decide to run this specific endpoint on the Vercel Edge Network (for faster global execution), the native Node dns module will not be supported. It runs perfectly fine on standard Node.js serverless functions, but it's worth keeping in mind for future scaling.


-------
Action Items / Architectural Critique

    JWT Signature Spoofing (Security/Cost Vector): Currently, the middleware manually decodes the base64 payload without cryptographically verifying the signature (e.g., using jwt.verify). Because of this, a malicious actor could forge a token containing an arbitrary userId, tricking your middleware into performing useless Prisma queries. While this won't grant them access (since the API routes are secured), it creates a minor vector for database exhaustion.

    Vercel Edge Compatibility: You are using Buffer.from(...) to decode the base64 string. Buffer is a Node.js-specific API. If you intend to deploy your Nuxt middleware to the Vercel Edge Network for faster global resolution, this will likely throw an error. Consider switching to the standard Web API atob() or a lightweight, Edge-compatible JWT decoder.

    SSR Database Latency: Executing a database query on every single SSR page load inherently adds latency to the initial render. Since your client-side interceptors already handle 401s robustly, you might want to monitor this. If database read limits become a bottleneck on Vercel, you could rely purely on cryptographic token verification here instead of hitting the database every time.

-------
server/utils/prisma.ts
Connection Exhaustion Risk: The current new Pool({ connectionString }) implementation relies on default connection parameters. In a serverless environment where functions scale horizontally per request, this can rapidly exhaust your database connection limits.

Required Verification: You must verify that the DATABASE_URL environment variable is strictly pointing to a connection pooler (e.g., Vercel's pooled connection string URL) rather than a direct database connection. If it points to a direct connection, you will encounter 500 Internal Server Error connection drops under concurrent user load.

Reference: Official Prisma Documentation regarding Driver Adapters and Serverless architectures emphasizes that managing connection pools via the pg driver natively in serverless functions requires an external pooler. My confidence level in this risk assessment is high based on standard serverless PostgreSQL constraints.

-------
app.vue, error.vue, pwa-types.d.ts
Architecture & UI Critique

    Hydration Cloak (app.vue): The delayed spinner mechanism (opacity 0 for the first 200ms) is an excellent UX pattern. It prevents jarring layout shifts and flickering for users with fast connections while providing necessary feedback on slower devices.

    Background Security (app.vue): Hooking into the visibilitychange event to verify the session_status shadow cookie is a highly robust approach for a PWA. It cleanly handles scenarios where the system revokes the session while the app is suspended in the background on a mobile device.

    Error Boundary (error.vue): The "Sovereign-Grade Recovery Protocol" aesthetic is consistent and visually striking. Using window.location.href = '/auth' instead of the Vue router to handle critical errors is a smart, defensive move to completely flush corrupted client states.

    Directory Structure: The image confirms a pristine Nuxt 4 setup. Centralizing everything under the app directory (assets, components, composables, layouts, locales, pages, plugins, stores, utils) keeps the root environment clean and scalable.

-------
ActiveCategoryCard.vue, ArticleReaderModal.vue, AuthInput.vue, InactiveCategoryCard.vue, LanguageSelector.vue, LogoutModal.vue, NewsCard.vue, PaywallModal.vue, PwaInstall.vue, RatingModal.vue

Action Items & Code Critique

    Z-Index Collisions (PwaInstall.vue vs. Modals): The PwaInstall.vue toast has z-index: 9999, while your highest modals (like LanguageSelector.vue) are around z-[200]. This means the PWA install prompt will render over your full-screen backdrops, which might look jarring or obscure critical modal content.

        Fix: Consider lowering the PWA toast's z-index (e.g., z-[150]) or ensuring modals that block interaction hide the toast temporarily.

    Missing Emits Definition (AuthInput.vue): In AuthInput.vue, you handle @blur="$emit('blur')", but 'blur' is not registered in the defineEmits array (defineEmits(['update:modelValue'])). In Vue 3, all emitted events should be explicitly declared to prevent them from falling through to the root element as native listeners, which can cause unexpected behavior.

        Fix: Update the setup script: defineEmits(['update:modelValue', 'blur']).

    Accessibility (A11y) Gaps: Across several components (e.g., NewsCard.vue, LanguageSelector.vue), icon-only buttons (like the more_vert menu toggle or the modal close buttons) lack aria-label attributes. Screen readers will not be able to interpret the purpose of these buttons.

        Fix: Add aria-label="Close" or aria-label="More options" to icon-only <button> elements.

--------
app-layout.vue
Action Items & Code Critique

    SSR Compatibility (vClickOutside): Your custom directive interacts directly with document.body inside the mounted and unmounted hooks. In a Nuxt SSR environment, accessing the DOM outside of import.meta.client or specific Vue lifecycle bounds can occasionally trigger hydration mismatches or server-side crashes if the layout attempts to render on the server.

        Fix: Ensure the event listeners are strictly wrapped: if (import.meta.client) { document.body.addEventListener(...) }. Alternatively, a simpler and often more performant pattern is to place a full-screen, invisible div behind the dropdown menu that closes the menu when clicked (@click="isMenuOpen = false").

    Accessibility (A11y) on Navigation: The header contains a button solely wrapping a Material icon (more_vert) to toggle the dropdown menu. Screen readers require context to understand this interactive element.

        Fix: Add an accessibility label and state indicator to the button: <button aria-label="Open user menu" :aria-expanded="isMenuOpen" ...>.

    Image Asset Referencing: You are using <img src="~/assets/images/NuSift_Transparent_Logo.png" />. While ~/assets works in Nuxt, Vite sometimes handles dynamic asset paths better when placed in the public directory (referenced simply as /images/NuSift_Transparent_Logo.png), especially for static logos that do not require Webpack/Vite processing or bundling.


--------
app/pages -> index.vue + password.vue
Akciópontok / Mérnöki Észrevételek

    Globális Stílusok Szétválasztása (index.vue): Az index.vue fájl <style> blokkjában definiált .standard-field input és button:active szabályok globálisan hatnak, mivel nincsenek scope-olva. Bár ez egy tudatos döntésnek tűnik a UI konzisztencia elérésére, architekturálisan tisztább lenne ezeket az alapszabályokat egy dedikált assets/css/main.css fájlba kiszervezni (amit a nuxt.config.ts hív be), vagy beépíteni a Tailwind alaprétegébe (@layer base).

    Jelszókezelő Integráció (reset-password.vue): A biztonság és a natív felhasználói élmény javítása érdekében az input mezőkhöz (új jelszó és megerősítés) érdemes hozzáadni az autocomplete="new-password" attribútumot. Ez explicit módon jelzi a böngészők beépített jelszókezelőinek (pl. Apple Keychain vagy Google Password Manager), hogy itt egy új hitelesítő adat generálása történik, így azonnal felajánlják a mentést.

    Aszinkron Állapotkezelés (reset-password.vue): A jelszó sikeres megváltoztatása után egy 3 másodperces setTimeout indítja el a visszanavigálást a bejelentkező felületre. Szakmai bevált gyakorlat, hogy ennek a timernek a referenciáját elmentjük, és egy onBeforeUnmount hookban futtatunk rá egy clearTimeout-ot. Ezzel megelőzhető, hogy a háttérben beragadjon egy időzítő, ha a felhasználó a 3 másodperc lejárta előtt türelmetlenségből manuálisan elnavigálna az oldalról.

-------
app/pages/audit
Action Items & Engineering Critique

    Optimistic UI State Flaw (categories-setup.vue): The local agentStore.topInterests array is mutated immediately before the $api POST request is dispatched. If the network request fails due to a timeout or server error, the frontend store will falsely reflect saved changes, falling out of synchronization with the database.

        Fix: Move the Pinia store mutations and the localStorage.setItem execution to occur strictly after the await $api(...) call resolves successfully. Alternatively, wrap the execution in a try-catch block that caches the original state and reverts the mutation if the API throws an error.

    Design System Leakage (source-manager.vue): The badge rendering switch statement hardcodes arbitrary hex colors and opacities (e.g., bg-[#3a3002]/40, text-[#b9ebf5]) directly into the component markup.

        Fix: To maintain strict adherence to the design system established in your tailwind.config.js, these status-specific colors should be abstracted into semantic Tailwind theme tokens (e.g., bg-status-pending, text-status-verified).

    State Broadcasting Architecture (categories-setup.vue): The utilization of globalSaveTick.value++ to broadcast save execution cycles to child components is an excellent engineering pattern. It keeps the child cards decoupled while allowing them to cleanly reset their internal "modified" tracking states.

--------
app/pages/auth/index.vue
Action Items & Engineering Critique

    Hardcoded Environment Configurations: The Apple Sign-In redirectURI is strictly hardcoded to https://nusift.io/api/auth/apple/callback within the handleOAuth function. This configuration will cause the Apple OAuth flow to fail in local development and staging environments.

        Fix: Extract this URI into useRuntimeConfig().public.appleRedirectUri to ensure environment-agnostic execution, similar to how you handled the Google Client ID.

    Memory Leak Risk (OAuth Timer): The safetyTimer initiates a 30-second countdown during the Google OAuth handshake. If the user forces a route change or the component is destroyed before the promise resolves or times out, the timer will still execute in the background, attempting to mutate unmounted refs (isLoading.value = false, etc.).

        Fix: Implement an onBeforeUnmount lifecycle hook that explicitly invokes clearSafetyTimer() to destroy the timeout instance upon component unmount.

    Third-Party Script Optimization: The useHead block unconditionally injects the external Google and Apple SDK scripts as soon as the authentication page mounts. While async and defer mitigate render blocking, they still consume network bandwidth.

        Fix: To further optimize initial load performance, consider dynamically injecting these SDKs only when the user interacts with (e.g., @mouseenter, @focus) the OAuth provider buttons or the surrounding container.

-------
app/pages/calibration 
Action Items & Engineering Critique

    Data Availability Race Condition (region-calibration.vue): Because the seed-region API call is unawaited and runs in the background, there is a race condition. If the user clicks "Continue" and loads the source-calibration.vue page faster than the Vercel serverless function finishes fetching and saving the GDELT/Newsdata APIs, the user will be presented with an empty source list.

        Fix: Consider adding a polling mechanism or a WebSocket event on the source-calibration page that listens for the seeder's completion, displaying a "Building your regional network..." skeleton loader until the data is actually available in the database.

    JWT Expiration Mismatch (interest-calibration.vue): In interest-calibration.vue, completing the onboarding immediately updates authStore.user.onboardingStep = 3 and saves the profile to localStorage. However, earlier we saw in your backend auth logic that the HTTP-Only JWT's expiration (1h vs 7d) is determined at the time of token generation. The finalize-onboarding API does not appear to issue a new JWT. This means the user's cookie will still expire in 1 hour despite the frontend recognizing them as fully onboarded.

        Fix: The /api/user/finalize-onboarding endpoint should ideally sign and attach a fresh 7-day JWT via setCookie to match the newly acquired Step 3 status.

    Design Token Duplication (source-calibration.vue): The getBadges function duplicates the hardcoded raw hex colors (e.g., bg-[#194d56]/40, bg-[#3a3002]/40) that are also present in the source-manager.vue component.

        Fix: Centralize these colors into your tailwind.config.js under semantic names (e.g., bg-status-verified, bg-status-pending) to ensure the Sovereign-Grade aesthetic remains maintainable from a single source of truth.


--------
app/pages/dashboard
Action Items & Engineering Critique

    Memory Leak Risk (initiate-dashboard.vue): The setTimeout triggers a route replacement after 2500ms. If the user manually navigates away or the component is destroyed before this timer finishes, the callback will still execute in the background, potentially forcing an unexpected redirect.

        Fix: Capture the timeout reference (let siftTimer: ReturnType<typeof setTimeout>) and clear it within an onBeforeUnmount lifecycle hook.

    Optimistic UI Syncing (dashboard-main.vue): When handling the rating confirmation (handleConfirmRating), the local articles ref is mutated directly (targetArticle.score = newScore;). While this provides immediate visual feedback, there does not appear to be an accompanying API call in this specific function block to synchronize this new score with the backend database.

        Fix: Ensure this local mutation is paired with a background $api request to your /api/user/rate-article endpoint, wrapped in a try-catch block that reverts the local score change if the network request fails.

    Directive Hydration Mismatch (dashboard-main.vue): The custom v-click-outside directive is used on the date dropdown. As noted in the layout analysis, ensure that this directive is strictly registered to only interact with the DOM on the client side (import.meta.client) to prevent Nuxt SSR hydration errors.

--------
app/pages/preloader 
Action Items & Engineering Critique

    Untracked Timeout Risk (preloader-second.vue): While you correctly clear the setInterval on unmount, the 3.5-second setTimeout that triggers Maps.replace("/dashboard-initiate") is fired anonymously and left untracked. If the user manually navigates away or the component is destroyed before those 3.5 seconds elapse, the timeout will still execute in the background and force an unexpected redirect.

        Fix: Capture the timeout reference (let navigationTimer: ReturnType<typeof setTimeout>) and invoke clearTimeout(navigationTimer) within your existing onBeforeUnmount hook.

    CSS Animation Duplication: Both preloader components define heavy, complex keyframe animations locally within their <style scoped> blocks. Since these elements form the core visual identity of the app's loading states, extending your tailwind.config.js to include these custom animations (theme.extend.animation and theme.extend.keyframes) would be architecturally cleaner and reduce the localized component footprint.

    Route-Based Preloading vs. Component State: Splitting the preloader phases across distinct URL routes (/preloader-page and /initialization-preloader-page) adds unnecessary entries to the router history and incurs slight rendering overhead.

        Consideration: It is often more performant to handle these phases within a single preloader route using a local reactive state (e.g., const currentPhase = ref<1 | 2>(1)), switching the UI dynamically before making the final push to the dashboard.


--------
app/pages/profile 
Action Items & Engineering Critique

    Critical Routing Deadlock (Guard Conflict): The "Adjust Agent" link points directly to /interest-calibration. However, your main-guard.global.ts explicitly includes /interest-calibration in the LOCKED_ONBOARDING_ROUTES array. If a fully onboarded user (onboardingStep >= 3) clicks this link, the global middleware will intercept the navigation and instantly force them back to the dashboard.

        Fix: You must either create a separate /profile/edit-interests route for post-onboarding modifications, or pass a specific query parameter (e.g., ?edit=true) that the global guard detects and explicitly allows to bypass the lockdown.

    Server-Side Session Ghosting (handleLogout): The local handleLogout function in this profile component only clears the client-side auth_token cookie and Pinia state before hard-redirecting to /auth. It completely skips calling the backend /api/auth/logout endpoint. Because your backend sets the JWT as an HTTP-Only cookie, frontend JavaScript cannot actually delete it. The user will be redirected to /auth, but the server will still consider their session active until the token naturally expires.

        Fix: Replace this local logic by triggering your centralized authStore.logoutIdentity() method (which safely hits the backend endpoint), or rely on the LogoutModal mechanism you already built into the app-layout.vue.

    Image Asset Handling: Importing defaultAvatar directly from ~/assets/images/ forces Vite to bundle the image into the JavaScript chunk during SSR. For static UI assets like default avatars, moving the image to the public/ directory and referencing it via a standard absolute path (e.g., /images/default_avatar.png) reduces JavaScript bundle size and improves caching.

------
app/pages/verifyEmail 
Action Items & Engineering Critique

    Unmanaged Interval Risk (verify-email.vue): The 60-second cooldown timer for resending emails uses setInterval without capturing its reference globally within the script scope. If the user navigates away from the page before the timer reaches 0, the interval continues running in the background, attempting to mutate unmounted reactive variables.

        Fix: Store the timer reference (let cooldownTimer: ReturnType<typeof setInterval>) and implement an onBeforeUnmount hook to execute clearInterval(cooldownTimer).

    State Persistence Edge Case (verify-email.vue): The resendEmail function relies entirely on localStorage.getItem("nusift_pending_email"). If a user registers on Desktop, opens the email on their Mobile device, fails verification, and tries to resend from Mobile, localStorage will be empty. The function will silently fail or show a generic error.

        Fix: Implement a fallback UI state. If pendingEmail is null, display a standard <input type="email"> field allowing the user to manually enter the address they are trying to verify.

    HTTP Client Inconsistency (verify.vue): The verification component uses Nuxt's native $fetch (const response = await $fetch<any>('/api/auth/verify'...)), bypassing the custom $api wrapper configured in api.ts that includes your global "Sovereign Shield" error interceptors. While this is a public endpoint, utilizing the $api instance ensures consistent networking behavior across the entire client application.


